---
name: discuss
description: "Interview the user one question at a time until the intent and context behind a task are settled, then hand off a plan in the conversation."
argument-hint: "<what you want to build or change>"
disable-model-invocation: true
---

Call the Skill tool with "discussing". The plan stays in the conversation; nothing is written to disk. The next step to name once the plan is confirmed is `/implement`.
